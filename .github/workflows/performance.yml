name: Performance

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: performance-${{ github.event.pull_request.number }}
  cancel-in-progress: true

env:
  BENCHMARK_THRESHOLD_PERCENT: 15

jobs:
  benchmark:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout baseline
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          path: baseline

      - name: Checkout candidate
        uses: actions/checkout@v6
        with:
          path: candidate

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Install baseline dependencies
        run: bun install --frozen-lockfile
        working-directory: baseline

      - name: Install candidate dependencies
        run: bun install --frozen-lockfile
        working-directory: candidate

      - name: Build baseline
        run: bun run build
        working-directory: baseline

      - name: Build candidate
        run: bun run build
        working-directory: candidate

      - name: Benchmark baseline
        run: >-
          bun candidate/scripts/benchmark.ts
          --module baseline/dist/index.js
          --json baseline-results.json

      - name: Benchmark candidate
        run: >-
          bun candidate/scripts/benchmark.ts
          --module candidate/dist/index.js
          --json candidate-results.json

      - name: Enforce performance budget
        run: >-
          bun candidate/scripts/compare-benchmarks.ts
          baseline-results.json
          candidate-results.json
